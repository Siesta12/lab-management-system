<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nlt.mapper.ReservationAuditLogMapper">
    <select id="selectPage" resultType="com.nlt.domain.entity.ReservationAuditLogEntity">
        select *
        from reservation_audit_log
        where 1 = 1
        <if test="reservationId != null">
            and reservation_id = #{reservationId}
        </if>
        <if test="auditUserId != null">
            and audit_user_id = #{auditUserId}
        </if>
        order by id desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countPage" resultType="long">
        select count(*)
        from reservation_audit_log
        where 1 = 1
        <if test="reservationId != null">
            and reservation_id = #{reservationId}
        </if>
        <if test="auditUserId != null">
            and audit_user_id = #{auditUserId}
        </if>
    </select>

    <select id="selectById" resultType="com.nlt.domain.entity.ReservationAuditLogEntity">
        select *
        from reservation_audit_log
        where id = #{id}
    </select>

    <select id="selectByReservationId" resultType="com.nlt.domain.entity.ReservationAuditLogEntity">
        select *
        from reservation_audit_log
        where reservation_id = #{reservationId}
        order by id desc
    </select>

    <insert id="insert"
                    parameterType="com.nlt.domain.entity.ReservationAuditLogEntity"
                    useGeneratedKeys="true"
                    keyProperty="id">
        insert into reservation_audit_log(
            reservation_id,
            audit_user_id,
            audit_action,
            audit_comment
        )
        values(
            #{reservationId},
            #{auditUserId},
            #{auditAction},
            #{auditComment}
        )
    </insert>
</mapper>

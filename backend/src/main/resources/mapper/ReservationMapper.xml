<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nlt.mapper.ReservationMapper">
    <select id="selectPage" resultType="com.nlt.domain.entity.ReservationEntity">
        select r.*
        from lab_reservation r
        join lab l on l.id = r.lab_id
        where 1 = 1
          and l.deleted = 0
        <if test="reservationNo != null and reservationNo != ''">
            and r.reservation_no like concat('%', #{reservationNo}, '%')
        </if>
        <if test="labId != null">
            and r.lab_id = #{labId}
        </if>
        <if test="applicantUserId != null">
            and r.applicant_user_id = #{applicantUserId}
        </if>
        <if test="approverUserId != null">
            and r.approver_user_id = #{approverUserId}
        </if>
        <if test="status != null">
            and r.status = #{status}
        </if>
        <if test="reservationDate != null">
            and exists (
                select 1
                from lab_reservation_slot s
                where s.reservation_id = r.id
                  and s.reservation_date = #{reservationDate}
            )
        </if>
        <if test="departmentId != null">
            and l.department_id = #{departmentId}
        </if>
        <if test="conflictOnly">
            and exists (
                select 1
                from lab_reservation_slot s
                where s.reservation_id = r.id
                  and s.slot_status = 1
                  and exists (
                      select 1
                      from lab_reservation_slot s2
                      join lab_reservation r2 on r2.id = s2.reservation_id
                      where s2.slot_status = 1
                        and s2.lab_id = s.lab_id
                        and s2.reservation_date = s.reservation_date
                        and s2.period_id = s.period_id
                        and r2.id &lt;&gt; r.id
                        and r2.status in (1, 2, 5)
                  )
            )
        </if>
        order by r.id desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countPage" resultType="long">
        select count(*)
        from lab_reservation r
        join lab l on l.id = r.lab_id
        where 1 = 1
          and l.deleted = 0
        <if test="reservationNo != null and reservationNo != ''">
            and r.reservation_no like concat('%', #{reservationNo}, '%')
        </if>
        <if test="labId != null">
            and r.lab_id = #{labId}
        </if>
        <if test="applicantUserId != null">
            and r.applicant_user_id = #{applicantUserId}
        </if>
        <if test="approverUserId != null">
            and r.approver_user_id = #{approverUserId}
        </if>
        <if test="status != null">
            and r.status = #{status}
        </if>
        <if test="reservationDate != null">
            and exists (
                select 1
                from lab_reservation_slot s
                where s.reservation_id = r.id
                  and s.reservation_date = #{reservationDate}
            )
        </if>
        <if test="departmentId != null">
            and l.department_id = #{departmentId}
        </if>
        <if test="conflictOnly">
            and exists (
                select 1
                from lab_reservation_slot s
                where s.reservation_id = r.id
                  and s.slot_status = 1
                  and exists (
                      select 1
                      from lab_reservation_slot s2
                      join lab_reservation r2 on r2.id = s2.reservation_id
                      where s2.slot_status = 1
                        and s2.lab_id = s.lab_id
                        and s2.reservation_date = s.reservation_date
                        and s2.period_id = s.period_id
                        and r2.id &lt;&gt; r.id
                        and r2.status in (1, 2, 5)
                  )
            )
        </if>
    </select>

    <select id="selectById" resultType="com.nlt.domain.entity.ReservationEntity">
        select *
        from lab_reservation
        where id = #{id}
    </select>

    <select id="selectMine" resultType="com.nlt.domain.entity.ReservationEntity">
        select *
        from lab_reservation
        where applicant_user_id = #{userId}
        order by id desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countMine" resultType="long">
        select count(*)
        from lab_reservation
        where applicant_user_id = #{userId}
    </select>

    <select id="selectPendingAudit" resultType="com.nlt.domain.entity.ReservationEntity">
        select r.*
        from lab_reservation r
        join lab l on l.id = r.lab_id
        left join sys_user u on u.id = r.applicant_user_id
        where r.status = 1
        <if test="departmentId != null">
            and l.department_id = #{departmentId}
        </if>
        order by case r.reservation_type
            when 1 then 1
            when 2 then 2
            else 3
        end asc,
        u.credit_score desc,
        r.created_at asc,
        r.id asc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countPendingAudit" resultType="long">
        select count(*)
        from lab_reservation r
        join lab l on l.id = r.lab_id
        where status = 1
        <if test="departmentId != null">
            and l.department_id = #{departmentId}
        </if>
    </select>

    <insert id="insert"
            parameterType="com.nlt.domain.entity.ReservationEntity"
            useGeneratedKeys="true"
            keyProperty="id">
        insert into lab_reservation(
            reservation_no,
            lab_id,
            applicant_user_id,
            approver_user_id,
            reservation_type,
            priority_level,
            usage_purpose,
            course_or_project_name,
            participant_count,
            contact_phone,
            status,
            reject_reason,
            check_in_time,
            check_out_time
        )
        values(
            #{reservationNo},
            #{labId},
            #{applicantUserId},
            #{approverUserId},
            #{reservationType},
            #{priorityLevel},
            #{usagePurpose},
            #{courseOrProjectName},
            #{participantCount},
            #{contactPhone},
            #{status},
            #{rejectReason},
            #{checkInTime},
            #{checkOutTime}
        )
    </insert>

    <update id="updateAuditResult" parameterType="com.nlt.domain.entity.ReservationEntity">
        update lab_reservation
        set approver_user_id = #{approverUserId},
            status = #{status},
            reject_reason = #{rejectReason}
        where id = #{id}
    </update>

    <update id="cancel">
        update lab_reservation
        set status = 4
        where id = #{id}
    </update>

    <update id="checkIn">
        update lab_reservation
        set check_in_time = now()
        where id = #{id}
    </update>

    <update id="checkOut">
        update lab_reservation
        set check_out_time = now(),
            status = 5
        where id = #{id}
    </update>

    <select id="selectDashboardDailySlots" resultType="com.nlt.domain.vo.dashboard.DashboardReservationItemVo">
        select s.reservation_id as reservationId,
               r.reservation_no as reservationNo,
               r.lab_id as labId,
               l.lab_name as labName,
               r.applicant_user_id as applicantUserId,
               u.real_name as applicantName,
               u.credit_score as applicantCreditScore,
               r.reservation_type as reservationType,
               r.priority_level as priorityLevel,
               r.status as status,
               date_format(s.reservation_date, '%Y-%m-%d') as reservationDate,
               s.period_id as periodId,
               p.period_no as periodNo,
               p.period_name as periodName,
               date_format(p.start_time, '%H:%i') as startTime,
               date_format(p.end_time, '%H:%i') as endTime,
               date_format(r.created_at, '%Y-%m-%d %H:%i:%s') as createdAt
        from lab_reservation_slot s
        join lab_reservation r on r.id = s.reservation_id
        join lab l on l.id = r.lab_id
        join sys_user u on u.id = r.applicant_user_id
        join class_period p on p.id = s.period_id
        where s.slot_status = 1
          and r.status in (1, 2, 5)
          and s.reservation_date = #{date}
        <if test="departmentId != null">
          and l.department_id = #{departmentId}
        </if>
        order by case r.status
                     when 1 then 1
                     when 2 then 2
                     when 5 then 3
                     else 4
                 end asc,
                 p.start_time asc,
                 r.priority_level asc,
                 u.credit_score desc,
                 r.created_at asc,
                 r.id asc,
                 p.sort_order asc
    </select>

    <!-- Statistics: all date-range filters use slot.reservation_date -->
    <select id="countByReservationType" resultType="map">
        select r.reservation_type as name, count(distinct r.id) as count
        from lab_reservation r
        join lab_reservation_slot s on s.reservation_id = r.id
        where 1 = 1
        <if test="startDate != null">
            and s.reservation_date <![CDATA[ >= ]]> #{startDate}
        </if>
        <if test="endDate != null">
            and s.reservation_date <![CDATA[ <= ]]> #{endDate}
        </if>
        <if test="labId != null">
            and r.lab_id = #{labId}
        </if>
        group by r.reservation_type
    </select>

    <select id="countByStatus" resultType="map">
        select r.status as name, count(distinct r.id) as count
        from lab_reservation r
        join lab_reservation_slot s on s.reservation_id = r.id
        where 1 = 1
        <if test="startDate != null">
            and s.reservation_date <![CDATA[ >= ]]> #{startDate}
        </if>
        <if test="endDate != null">
            and s.reservation_date <![CDATA[ <= ]]> #{endDate}
        </if>
        <if test="labId != null">
            and r.lab_id = #{labId}
        </if>
        group by r.status
    </select>

    <select id="countByLabUsage" resultType="map">
        select l.lab_name as name, count(distinct r.id) as count
        from lab l
        left join lab_reservation r on l.id = r.lab_id
        left join lab_reservation_slot s on s.reservation_id = r.id
        where l.deleted = 0
        <if test="departmentId != null">
            and l.department_id = #{departmentId}
        </if>
        <if test="labId != null">
            and l.id = #{labId}
        </if>
        <if test="startDate != null">
            and (r.id is null or s.reservation_date <![CDATA[ >= ]]> #{startDate})
        </if>
        <if test="endDate != null">
            and (r.id is null or s.reservation_date <![CDATA[ <= ]]> #{endDate})
        </if>
        group by l.id, l.lab_name
        order by count desc, l.id asc
    </select>

    <!-- "Time distribution" becomes "period distribution" -->
    <select id="countByTimeDistribution" resultType="map">
        select p.period_name as name, count(*) as count
        from lab_reservation_slot s
        join lab_reservation r on r.id = s.reservation_id
        join class_period p on p.id = s.period_id
        where 1 = 1
        <if test="startDate != null">
            and s.reservation_date <![CDATA[ >= ]]> #{startDate}
        </if>
        <if test="endDate != null">
            and s.reservation_date <![CDATA[ <= ]]> #{endDate}
        </if>
        <if test="labId != null">
            and r.lab_id = #{labId}
        </if>
        group by p.id, p.period_name
        order by p.sort_order asc, p.period_no asc
    </select>

    <select id="reservationTrend" resultType="map">
        select date_format(s.reservation_date, '%Y-%m-%d') as name, count(distinct r.id) as count
        from lab_reservation_slot s
        join lab_reservation r on r.id = s.reservation_id
        where 1 = 1
        <if test="startDate != null">
            and s.reservation_date <![CDATA[ >= ]]> #{startDate}
        </if>
        <if test="endDate != null">
            and s.reservation_date <![CDATA[ <= ]]> #{endDate}
        </if>
        group by s.reservation_date
        order by s.reservation_date asc
    </select>
</mapper>
